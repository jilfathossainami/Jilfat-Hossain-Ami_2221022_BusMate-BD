databases:
  # 1. PostgreSQL Database
  - name: busmate-db
    plan: free
    region: singapore

services:
  # 2. Node.js Backend API
  - type: web
    name: busmate-backend
    env: node
    plan: free
    region: singapore
    buildCommand: cd backend && npm install --production=false && npx prisma generate && npx prisma db push && npx tsx prisma/seed.ts && npm run build
    startCommand: cd backend && npm run start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: busmate-db
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: https://busmate-frontend.onrender.com
      - key: AI_API_KEY
        value: '' # Add your Gemini API key here in the Render dashboard
      - key: GROQ_API_KEY
        value: '' # Add your Groq API key here for real AI responses

  # 3. React Frontend (Static Site)
  - type: web
    name: busmate-frontend
    env: static
    buildCommand: cd frontend && npm install && npm run build
    staticPublishPath: frontend/dist
    envVars:
      - key: VITE_API_URL
        value: https://busmate-backend.onrender.com
      - key: VITE_SOCKET_URL
        value: https://busmate-backend.onrender.com
      - key: VITE_APP_NAME
        value: BusMate BD
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
